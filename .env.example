# Copy this file to `.env` and fill in the values to enable the optional
# AI-assisted formatting workflow. These values are read at runtime using
# environment variables so that secrets never need to be committed to the
# repository.

# API key for the OpenAI account that has access to GPT-4o Vision models.
OPENAI_API_KEY=

# Fully qualified model name, for example `gpt-4o-mini` or `gpt-4.1`.
OPENAI_API_MODEL=

# Optional override for non-default API endpoints. Leave blank to use the
# public OpenAI API.
# OPENAI_API_BASE_URL=https://api.openai.com/v1

# Control whether intermediate artifacts (plain text exports, AI JSON
# instructions, and formatted DOCX files) are packaged alongside the final
# XML bundle.
# EXPORT_INTERMEDIATE_ARTIFACTS=true
